<%= render "shared/second_header" %>
<div class='form-body'>
  <h1 class ='top-message pt-4 font-weight-bold'>ようこそ、Easy Boardへ</h1>
  <h3 class ='top-message mt-3 mb-4'>必要な情報は人ぞれぞれ　必要な情報だけ届く掲示板</h3>
  <div class='form-main'>
    <div class='forms text-center m-auto'>
      <h1 class = 'font-weight-bold mb-5'>ログイン</h1>
      <div class='login-flash-message'>
        <%= flash[:notice] %>
        <%= flash[:alert] %>
      </div>
      <%= form_with model: @user, url: user_session_path, id: 'new_user', class: 'new_user', local: true do |f| %>
        <div class='form'>
          <h3 class='form-label'>
            <%= f.label :メールアドレス%>
          </h3>
          <div>
            <%= f.email_field :email, class: 'form-input', placeholder: '登録したメールアドレスを入力', autofocus: true%>
          </div>
        </div>
        <div class='form'>
          <h3 class='form-label'>
            <%= f.label :パスワード%>
          </h3>
          <div>
            <%= f.password_field :password, class: 'form-input', placeholder: '英数字混合6文字以上', autocomplete: "off"%>
          </div>
        </div>
        <div class='submit pt-4'>
          <%= f.submit "ログイン", class: 'btn btn-primary px-5 font-weight-bold'%>
        </div>
      <% end %>
    </div>
  </div>
</div>
